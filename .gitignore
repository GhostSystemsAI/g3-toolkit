# === Build artifacts ===
# ROOT-ANCHORED deliberately. Any-depth `dist/` and `coverage/` rules
# silently swallowed SOURCE (tests/dist/public-api.test.ts, which is
# how verify:exports lost its tests; packages/react/src/views/coverage/,
# which broke CI typecheck on first push; scripts/storybook-static/).
# If a new artifact dir appears somewhere deeper, add its explicit
# path; never a bare directory name.
/dist/
/build/
docs/build/
packages/*/dist
packages/*/tsconfig.tsbuildinfo

# === Sphinx copies (from build_docs.py) ===
docs/source/specs/
docs/source/research/
docs/source/planning/

# === Dependencies ===
node_modules/
.pnpm-store/

# === Test outputs ===
test-results/
playwright-report/
/coverage/
/results/
# Screenshot baselines are committed; only ignore temp files
tests/e2e/__screenshots__/*-retry*

# === Storybook ===
*storybook.log
/storybook-static/

# === Vite / dev caches ===
.vite/
.cache/
.turbo/

# === Round deliverables ===
# Packaged zips and the handoff documents that ship with them. Both are
# point-in-time artifacts naming a specific commit, and the handoffs
# quote the remediation queue, which follows the same
# keep-it-out-of-the-repo rule as audit/. The zip is a 6 MB binary per
# round and does not belong in history either.
outputs/

# === pnpm / npm pack outputs ===
*.tgz
pnpm-debug.log
npm-debug.log

# === Python / pixi ===
.pixi/
__pycache__/
*.pyc
*.egg-info/
.env

# === Editor / OS noise ===
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.example.json
*.swp
*.swo

# Generated docs (TypeDoc + Storybook build)
docs-out/
.verify-types
.verify-snippets
test-results.json
tests/perf/last-results.json

# 
planning/archive/

# AI
.devcontainer/
.mcp.json
audit/
